# Agent Instructions

Before starting work in this repository, read `docs/codex-context.md` first. It contains the current architecture summary, key file locations, known risks, and implementation rules for Codex tasks.

Use that context as a starting point, then inspect the relevant source files and current diffs before making changes. The context file may lag behind the codebase, so prefer the live source when there is a mismatch.

Do not duplicate broad architecture notes here. Keep durable repo guidance in `docs/codex-context.md` and update that file when the inspected architecture or implementation rules materially change.
